export const PARENT_DOC = parent.document;

export const LIVE_PHOTO_TYPE = ":live-photo";

export const DEFAULT_SETTINGS = {
  regexPattern: "^(.*?)-([A-Za-z0-9]{5,6})\\.(jpg|jpeg|png|gif|mov|mp4)$",
  enableAutoConvert: false,
};

export const SETTINGS_KEY = "logseq-livephotos-settings";
